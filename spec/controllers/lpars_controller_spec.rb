require_relative '../spec_helper'

describe LparsController do
  render_views
  
  describe "GET #index" do
      it "populates an array of lpars" do
#        lpar = build(:lpar)
#        get :index
        #assigns(:lpars).should eq([lpar])
    end
      it "renders the :index view" do
#        get :index
        #response.should render_template :index
    end
  end
end