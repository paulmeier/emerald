= simple_form_for @mod, html: { class: 'form-horizontal' } do |f|
  .form-inputs
    = f.input :name
    = f.input :books
  .form-actions
    = f.button :submit, class: 'btn-primary'
    = link_to t('.cancel', default: t("helpers.links.cancel")), mods_path, class: 'btn'