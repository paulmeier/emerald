= simple_form_for @notification, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :msg
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), notifications_path, :class => 'btn'
